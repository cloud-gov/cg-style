
{{#each organizations}}
<div class="panel-row panel-row-boxed"> <!-- card--home--quicklook -->
  <div class="NNNNN"> <!-- card--home-header -->
    <div class="panel-column"> <!-- card--home-header-title -->
      <h2 class="sans-s7 card--home-title">
        <svg class="icon-fill icon-fill-inactive">
          <use xlink:href="/img/cloudgov-sprite.svg#i-fill-org"></use>
        </svg>
        <a href="#">{{ name }}</a>
      </h2>
    </div> <!-- card--home-header-title -->
    <div class="panel-column"> <!-- card--home-header-status -->

      <span class="count_status count_status-none"> <!-- card--home-header-status-entity -->
        <span class="count_status-icon">
          <svg class="icon-fill icon-bordered icon-fill-alt">
            <use xlink:href="/img/cloudgov-sprite.svg#i-fill-space"></use>
          </svg>
        </span>
        <span class="count_status-text">
          {{#if spaces.0.name}}
            <strong>{{ spaces.length }}</strong> space{{#if spaces.1.name}}s{{/if}}
          {{else}}
            <strong>0</strong> spaces
          {{/if}}
        </span>
      </span><!-- /card--home-header-status-entity -->

      <span class="count_status count_status-started"> <!-- card--home-header-status-item -->
        <span class="count_status-icon">
          <svg class="icon-fill icon-bordered icon-ok">
            <use xlink:href="/img/cloudgov-sprite.svg#i-fill-app"></use>
          </svg>
        </span>
        <span class="count_status-text">
          {{#if apps.0.name}}
            <strong>{{ apps.length }}</strong> app{{#if apps.1.name}}s{{/if}}
          {{else}}
            <strong>0</strong> apps
          {{/if}}
        </span>
      </span><!-- /card--home-header-status-item -->

    </div> <!-- /card--home-header-status -->
  </div> <!-- /card--home--header -->
</div> <!-- /card--home--quicklook -->

{{/each}}
